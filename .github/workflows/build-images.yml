name: Build images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  WEB_IMAGE: ghcr.io/choi-seunghwan/saegim-web
  API_IMAGE: ghcr.io/choi-seunghwan/saegim-api

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: web
            image: ghcr.io/choi-seunghwan/saegim-web
            dockerfile: apps/web/Dockerfile
          - name: api
            image: ghcr.io/choi-seunghwan/saegim-api
            dockerfile: apps/api/Dockerfile

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ matrix.image }}
          tags: |
            type=sha,prefix=
            type=ref,event=branch

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            NEXT_PUBLIC_SITE_URL=https://saegim.chuz.dev
            NEXT_PUBLIC_API_BASE_URL=https://api-saegim.chuz.dev
            NEXT_PUBLIC_MIXPANEL_TOKEN=${{ vars.NEXT_PUBLIC_MIXPANEL_TOKEN }}
            NEXT_PUBLIC_MIXPANEL_ENABLED=${{ vars.NEXT_PUBLIC_MIXPANEL_ENABLED || 'false' }}
            NEXT_PUBLIC_MIXPANEL_DEBUG=${{ vars.NEXT_PUBLIC_MIXPANEL_DEBUG || 'false' }}
            NEXT_PUBLIC_MIXPANEL_API_HOST=${{ vars.NEXT_PUBLIC_MIXPANEL_API_HOST }}
            NEXT_PUBLIC_MIXPANEL_LIB_URL=${{ vars.NEXT_PUBLIC_MIXPANEL_LIB_URL }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}
